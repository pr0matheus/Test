{
  "name": "smart-office-dashboard",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node -e \"const fs = require('fs'); console.log('=> Env Check - MQTT_USER:', process.env.MQTT_USER ? 'LOADED' : 'MISSING'); const creds = JSON.stringify({'1c8b5a3527113f23':{user:process.env.MQTT_USER||'',password:process.env.MQTT_PASS||''}}); fs.writeFileSync('flows_cred.json', creds); try { fs.mkdirSync('/opt/render/.node-red', {recursive:true}); fs.writeFileSync('/opt/render/.node-red/flows_cred.json', creds); } catch(e){}\" && node-red -s settings.js flows.json"
  },
  "dependencies": {
    "node-red": "^3.1.15",
    "@flowfuse/node-red-dashboard": "^1.30.2",
    "node-red-node-email": "^5.2.4"
  }
}
